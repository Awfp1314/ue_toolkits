# 📁 UE Toolkit 项目结构说明

> 虚幻引擎资产管理工具箱 - 项目结构完整文档

---

## 📋 目录

- [项目概览](#项目概览)
- [核心目录结构](#核心目录结构)
- [详细说明](#详细说明)
- [模块依赖关系](#模块依赖关系)
- [数据流向](#数据流向)

---

## 项目概览

```
ue_toolkits/
├── 🎯 核心系统 (core/)
├── 🧩 功能模块 (modules/)
├── 🎨 用户界面 (ui/)
├── 📦 资源文件 (resources/)
├── 🚀 发布版本 (release/)
├── 🧪 测试代码 (tests/)
└── 📚 项目文档
```

**项目类型**：PyQt6 桌面应用  
**架构模式**：模块化 + MVC  
**打包工具**：PyInstaller

---

## 核心目录结构

```
ue_toolkits - ai/
│
├── 📄 main.py                              # 程序入口
├── 📄 requirements.txt                     # Python 依赖列表
├── 📄 ue_toolkit.spec                      # PyInstaller 打包配置
├── 📄 LICENSE                              # 开源许可证
├── 📄 .gitignore                           # Git 忽略配置
│
├── 📚 文档
│   ├── README.md                           # 项目说明（GitHub首页）
│   ├── RELEASE.md                          # 版本发布说明
│   ├── Ue_Toolkit使用文档.md               # 用户手册（简化版）
│   └── 虚幻资产工具箱使用文档.md           # 用户手册（完整版）
│
├── 🔧 构建脚本
│   ├── build.py                            # 开发构建脚本
│   ├── build_release.py                    # 正式发布构建脚本
│   ├── run_with_console.bat                # 开发运行（显示控制台）
│   └── run_without_console.bat             # 开发运行（隐藏控制台）
│
├── 🎯 core/                                # 核心系统
│   ├── __init__.py
│   ├── app_manager.py                      # 应用管理器（总调度）
│   ├── module_manager.py                   # 模块管理器
│   ├── module_interface.py                 # 模块接口定义
│   ├── base_logic.py                       # 基础逻辑类
│   ├── logger.py                           # 日志系统
│   ├── single_instance.py                  # 单例模式（防止多开）
│   ├── config_manager.py                   # 配置管理器（向后兼容）
│   │
│   ├── config/                             # 配置子系统
│   │   ├── config_manager.py               # 配置管理
│   │   ├── config_validator.py             # 配置验证
│   │   └── config_backup.py                # 配置备份
│   │
│   ├── config_templates/                   # 配置模板
│   │   └── app_config_template.json
│   │
│   └── utils/                              # 工具类
│       ├── path_utils.py                   # 路径工具
│       ├── file_utils.py                   # 文件操作
│       ├── validators.py                   # 验证器
│       ├── custom_widgets.py               # 自定义控件
│       ├── style_loader.py                 # 样式加载器
│       ├── theme_manager.py                # 主题管理器
│       ├── thread_utils.py                 # 线程工具
│       ├── ue_process_utils.py             # UE进程工具
│       └── performance_monitor.py          # 性能监控
│
├── 🧩 modules/                             # 功能模块（可插拔）
│   │
│   ├── _template/                          # 模块模板（开发参考）
│   │   ├── __init__.py
│   │   ├── __main__.py
│   │   ├── manifest.json                   # 模块元信息
│   │   ├── logic/
│   │   │   └── module_logic.py
│   │   └── ui/
│   │       └── __init__.py
│   │
│   ├── 🤖 ai_assistant/                    # AI 助手模块
│   │   ├── __init__.py
│   │   ├── __main__.py                     # 模块入口
│   │   ├── ai_assistant.py                 # 主类
│   │   ├── manifest.json                   # 模块配置
│   │   ├── README.md                       # 模块说明
│   │   ├── 智能上下文管理系统完整文档.md   # 技术文档
│   │   │
│   │   ├── logic/                          # 业务逻辑层
│   │   │   ├── api_client.py               # API 客户端
│   │   │   ├── context_manager.py          # 上下文管理器 ⭐
│   │   │   ├── enhanced_memory_manager.py  # 增强型记忆管理器 ⭐
│   │   │   ├── conversation_memory.py      # 对话记忆
│   │   │   ├── asset_reader.py             # 资产读取器
│   │   │   ├── config_reader.py            # 配置读取器
│   │   │   ├── document_reader.py          # 文档读取器
│   │   │   ├── log_analyzer.py             # 日志分析器
│   │   │   └── config.py                   # 配置
│   │   │
│   │   ├── ui/                             # 用户界面层
│   │   │   ├── chat_window.py              # 聊天窗口（主界面）
│   │   │   ├── chat_input.py               # 输入框
│   │   │   ├── chat_input_bar.py           # 输入栏
│   │   │   ├── message_bubble.py           # 消息气泡
│   │   │   └── markdown_message.py         # Markdown 消息
│   │   │
│   │   └── resources/                      # 资源文件
│   │       └── themes/
│   │           ├── dark.qss
│   │           └── light.qss
│   │
│   ├── 📦 asset_manager/                   # 资产管理模块
│   │   ├── __init__.py
│   │   ├── asset_manager.py                # 主类
│   │   ├── manifest.json
│   │   ├── config_template.json
│   │   │
│   │   ├── logic/                          # 业务逻辑
│   │   │   ├── asset_manager_logic.py      # 核心逻辑
│   │   │   ├── asset_model.py              # 资产模型
│   │   │   └── thumbnail_generator.py      # 缩略图生成
│   │   │
│   │   └── ui/                             # 用户界面
│   │       ├── asset_manager_ui.py         # 主界面
│   │       ├── asset_card.py               # 资产卡片
│   │       ├── add_asset_dialog.py         # 添加资产对话框
│   │       ├── edit_asset_dialog.py        # 编辑资产对话框
│   │       ├── first_launch_dialog.py      # 首次启动对话框
│   │       ├── set_paths_dialog.py         # 路径设置对话框
│   │       ├── category_management_dialog.py  # 分类管理
│   │       ├── confirm_delete_category_dialog.py
│   │       ├── dialogs.py                  # 其他对话框
│   │       ├── custom_checkbox.py          # 自定义复选框
│   │       └── progress_dialog.py          # 进度对话框
│   │
│   ├── ⚙️ config_tool/                     # 配置工具模块
│   │   ├── __init__.py
│   │   ├── __main__.py
│   │   ├── manifest.json
│   │   │
│   │   ├── logic/                          # 业务逻辑
│   │   │   └── config_tool_logic.py        # 核心逻辑
│   │   │
│   │   └── ui/                             # 用户界面
│   │       ├── config_tool_ui.py           # 主界面
│   │       ├── config_tool_ui_core.py      # 核心UI
│   │       │
│   │       ├── components/                 # UI组件
│   │       │   ├── config_item_widget.py
│   │       │   ├── project_card.py
│   │       │   ├── template_card.py
│   │       │   └── tree_builder.py
│   │       │
│   │       ├── dialogs/                    # 对话框
│   │       │   ├── add_project_dialog.py
│   │       │   ├── apply_template_dialog.py
│   │       │   ├── create_template_dialog.py
│   │       │   ├── edit_template_dialog.py
│   │       │   └── select_config_dialog.py
│   │       │
│   │       ├── handlers/                   # 事件处理
│   │       │   └── config_handler.py
│   │       │
│   │       └── layouts/                    # 布局
│   │           └── layout_builder.py
│   │
│   └── 🌐 site_recommendations/            # 网站推荐模块
│       ├── __init__.py
│       ├── __main__.py
│       ├── manifest.json
│       │
│       ├── logic/                          # 业务逻辑
│       │   └── site_recommendations_logic.py
│       │
│       └── ui/                             # 用户界面
│           └── site_recommendations_ui.py
│
├── 🎨 ui/                                  # 主界面系统
│   ├── __init__.py
│   ├── ue_main_window.py                   # 主窗口
│   ├── ue_main_window_core.py              # 主窗口核心
│   ├── settings_widget.py                  # 设置界面
│   ├── system_tray_manager.py              # 系统托盘
│   │
│   ├── dialogs/                            # 对话框
│   │   └── close_confirmation_dialog.py    # 关闭确认
│   │
│   ├── main_window_components/             # 主窗口组件
│   │   └── title_bar.py                    # 标题栏
│   │
│   ├── main_window_handlers/               # 事件处理
│   │   ├── module_loader.py                # 模块加载
│   │   └── navigation_handler.py           # 导航处理
│   │
│   └── icons/                              # 图标
│       └── toolbar.png
│
├── 📦 resources/                           # 资源文件
│   │
│   ├── qss/                                # 样式表
│   │   ├── main_window.qss                 # 主窗口样式
│   │   ├── sidebar.qss                     # 侧边栏样式
│   │   ├── config_tool.qss                 # 配置工具样式
│   │   ├── variables.qss                   # 变量定义
│   │   │
│   │   ├── components/                     # 组件样式
│   │   │   ├── buttons.qss
│   │   │   ├── dialogs.qss
│   │   │   ├── scrollbars.qss
│   │   │   └── title_bar.qss
│   │   │
│   │   └── themes/                         # 主题
│   │       ├── dark.qss                    # 深色主题
│   │       └── light.qss                   # 浅色主题
│   │
│   ├── themes/                             # 自定义主题
│   │   ├── README.md
│   │   ├── example_custom_theme.json
│   │   ├── forest_green.json
│   │   ├── sunset_orange.json
│   │   └── violet.json
│   │
│   ├── templates/                          # 配置模板
│   │   └── global_settings.json
│   │
│   └── tubiao.ico                          # 应用图标
│
├── 🧪 tests/                               # 单元测试
│   ├── __init__.py
│   ├── conftest.py                         # pytest 配置
│   └── test_core/                          # 核心测试
│       ├── test_config_validator.py
│       └── test_module_manager.py
│
└── 🚀 release/                             # 发布版本
    ├── run_with_console.bat                # 运行脚本（显示控制台）
    ├── run_without_console.bat             # 运行脚本（隐藏控制台）
    ├── version_info.txt                    # 版本信息
    └── ue_toolkit/                         # 打包后的程序
        ├── ue_toolkit.exe                  # 可执行文件
        └── _internal/                      # 依赖文件
```

---

## 详细说明

### 🎯 core/ - 核心系统

**职责**：提供底层基础设施和通用功能

| 文件 | 说明 | 关键功能 |
|------|------|---------|
| `app_manager.py` | 应用管理器 | 整个应用的生命周期管理 |
| `module_manager.py` | 模块管理器 | 动态加载/卸载模块 |
| `module_interface.py` | 模块接口 | 定义模块标准接口 |
| `base_logic.py` | 基础逻辑类 | 所有逻辑类的基类 |
| `logger.py` | 日志系统 | 统一的日志管理 |
| `single_instance.py` | 单例管理 | 防止程序多开 |

**config/ 子系统**：
- `config_manager.py` - 配置的增删改查
- `config_validator.py` - 配置验证
- `config_backup.py` - 配置备份恢复

**utils/ 工具集**：
- `path_utils.py` - 跨平台路径处理
- `file_utils.py` - 文件操作封装
- `theme_manager.py` - 主题切换
- `thread_utils.py` - 多线程工具

---

### 🧩 modules/ - 功能模块

**模块化设计特点**：
- ✅ 可插拔：每个模块可独立开发/测试
- ✅ 标准化：统一的 `manifest.json` 配置
- ✅ 分层明确：logic（逻辑）/ ui（界面）分离

#### 🤖 AI 助手模块 (ai_assistant/)

**核心价值**：智能上下文感知的 AI 对话助手

**关键组件**：

1. **ContextManager（上下文管理器）**
   - 7 层智能上下文融合
   - 自动检索相关数据
   - 动态注入程序状态

2. **EnhancedMemoryManager（记忆管理器）**
   - 多级记忆（USER/SESSION/CONTEXT）
   - 持久化存储
   - 智能检索

3. **数据读取器**
   - AssetReader - 读取资产数据
   - ConfigReader - 读取配置数据
   - LogAnalyzer - 分析日志
   - DocumentReader - 读取文档

**技术栈**：
- API 调用：OpenAI 兼容接口
- UI 框架：PyQt6
- 消息渲染：Markdown 支持

#### 📦 资产管理模块 (asset_manager/)

**核心价值**：虚幻引擎资产的导入、分类、管理

**主要功能**：
- 资产导入（文件/文件夹）
- 自动分类
- 缩略图生成
- 资产搜索
- 批量操作

**数据模型**：
```python
Asset:
  - name: 资产名称
  - category: 分类
  - asset_type: 类型（文件/文件夹）
  - path: 路径
  - thumbnail_path: 缩略图路径
  - description: 描述
```

#### ⚙️ 配置工具模块 (config_tool/)

**核心价值**：UE 项目配置的模板化管理

**主要功能**：
- 读取 UE 项目配置（.ini 文件）
- 保存为模板
- 应用模板到项目
- 配置对比
- 批量管理

**支持的配置类型**：
- DefaultEngine.ini
- DefaultGame.ini
- DefaultInput.ini
- 等所有 UE 配置文件

#### 🌐 网站推荐模块 (site_recommendations/)

**核心价值**：快速访问常用 UE 相关网站

**功能**：
- 分类网站列表
- 快速跳转
- 自定义添加

---

### 🎨 ui/ - 主界面系统

**职责**：应用的主窗口框架和通用 UI 组件

**核心组件**：

1. **ue_main_window.py** - 主窗口
   - 侧边栏导航
   - 模块内容区
   - 标题栏
   - 系统托盘

2. **settings_widget.py** - 设置界面
   - 主题切换
   - 全局配置
   - 模块设置

3. **模块加载器** (module_loader.py)
   - 动态加载模块 UI
   - 生命周期管理

**UI 特点**：
- 🎨 支持深色/浅色主题
- 🎯 自定义标题栏（无边框设计）
- 📱 响应式布局
- 🖱️ 系统托盘集成

---

### 📦 resources/ - 资源文件

**资源分类**：

| 类型 | 位置 | 说明 |
|------|------|------|
| **QSS 样式** | `qss/` | PyQt6 样式表 |
| **主题** | `themes/` | 自定义主题配置 |
| **模板** | `templates/` | 配置模板 |
| **图标** | `tubiao.ico` | 应用图标 |

**样式组织**：
```
qss/
├── main_window.qss       # 主窗口
├── sidebar.qss           # 侧边栏
├── config_tool.qss       # 配置工具
├── components/           # 可复用组件
│   ├── buttons.qss
│   ├── dialogs.qss
│   └── scrollbars.qss
└── themes/               # 主题变体
    ├── dark.qss
    └── light.qss
```

---

### 🚀 release/ - 发布版本

**内容**：
- `ue_toolkit.exe` - 打包后的可执行文件
- `_internal/` - Python 运行时和依赖
- `run_*.bat` - 启动脚本
- `version_info.txt` - 版本信息

**打包流程**：
```bash
python build_release.py
# 生成 release/ue_toolkit/
```

---

## 模块依赖关系

```
┌─────────────────────────────────────────────────┐
│                   main.py                       │
│              (程序入口)                          │
└─────────────────┬───────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────┐
│              AppManager                          │
│          (应用生命周期管理)                      │
└─────────────────┬───────────────────────────────┘
                  │
        ┌─────────┼─────────┐
        ↓         ↓         ↓
    ┌─────┐  ┌──────┐  ┌──────┐
    │ UI  │  │Module│  │Config│
    │     │  │Manager│ │      │
    └─────┘  └───┬──┘  └──────┘
                 │
      ┌──────────┼──────────┐
      ↓          ↓          ↓
┌──────────┐ ┌─────────┐ ┌──────────┐
│  Asset   │ │   AI    │ │  Config  │
│ Manager  │ │Assistant│ │   Tool   │
└──────────┘ └────┬────┘ └──────────┘
                   │
          ┌────────┼────────┐
          ↓        ↓        ↓
    ┌─────────┐ ┌──────┐ ┌─────┐
    │ Context │ │Memory│ │Asset│
    │ Manager │ │ Mgr  │ │Read │
    └─────────┘ └──────┘ └─────┘
```

**关键依赖**：
1. 所有模块依赖 `core/`
2. AI 助手依赖其他模块的 logic 层
3. UI 层不直接访问 logic 层（通过接口）

---

## 数据流向

### 用户操作流向

```
用户点击
    ↓
UI 事件处理
    ↓
调用 logic 层方法
    ↓
访问 core 工具类
    ↓
操作文件系统/数据库
    ↓
返回结果
    ↓
更新 UI
```

### AI 对话流向

```
用户输入查询
    ↓
ChatWindow.send_message()
    ↓
ContextManager.build_context()
    ├─ 检索记忆
    ├─ 读取资产数据
    ├─ 读取配置数据
    ├─ 分析日志
    └─ 融合上下文
    ↓
APIClient.send_request()
    ↓
接收 AI 回复
    ↓
保存到记忆
    ↓
显示在 UI
```

---

## 配置和数据存储

### 用户数据目录

**Windows**:
```
C:\Users\{用户名}\AppData\Local\UE_Toolkit\
├── config\                    # 配置文件
│   ├── app_config.json        # 应用配置
│   └── modules\               # 模块配置
│       ├── asset_manager.json
│       └── ai_assistant.json
├── logs\                      # 日志文件
│   └── ue_toolkit_YYYYMMDD.log
├── ai_memory\                 # AI 记忆
│   └── default_memory.json
└── asset_library\             # 资产库（默认）
```

**macOS**:
```
~/Library/Application Support/UE_Toolkit/
```

**Linux**:
```
~/.local/share/UE_Toolkit/
```

---

## 开发指南

### 如何添加新模块

1. **复制模块模板**
   ```bash
   cp -r modules/_template modules/my_module
   ```

2. **修改 manifest.json**
   ```json
   {
     "name": "my_module",
     "display_name": "我的模块",
     "version": "1.0.0",
     "description": "模块描述",
     "entry_point": "my_module",
     "dependencies": []
   }
   ```

3. **实现核心功能**
   - `logic/module_logic.py` - 业务逻辑
   - `ui/module_ui.py` - 用户界面

4. **模块会自动被加载**

### 如何添加新的 UI 样式

1. 在 `resources/qss/` 中创建 `.qss` 文件
2. 使用 `style_loader.py` 加载
3. 支持变量引用（见 `variables.qss`）

### 如何添加单元测试

1. 在 `tests/test_core/` 创建测试文件
2. 使用 pytest 框架
3. 运行测试：`pytest tests/`

---

## 构建和发布

### 开发构建
```bash
python build.py
```
- 输出：`dist/ue_toolkit/`
- 用于测试

### 正式发布构建
```bash
python build_release.py
```
- 输出：`release/ue_toolkit/`
- 包含版本信息
- 生成启动脚本

### 打包配置
- `ue_toolkit.spec` - PyInstaller 配置
- 自动包含所有依赖
- 生成单文件夹可执行程序

---

## 技术栈

| 分类 | 技术 | 说明 |
|------|------|------|
| **语言** | Python 3.11+ | 核心语言 |
| **UI 框架** | PyQt6 | 桌面界面 |
| **打包工具** | PyInstaller | 生成可执行文件 |
| **配置格式** | JSON | 配置文件格式 |
| **样式** | QSS | PyQt 样式表 |
| **测试** | pytest | 单元测试 |
| **日志** | logging | Python 标准库 |
| **AI 接口** | OpenAI API | AI 对话 |

---

## 性能指标

| 指标 | 值 | 说明 |
|------|-----|------|
| 启动时间 | < 2s | 冷启动 |
| 内存占用 | ~100MB | 空闲状态 |
| 打包大小 | ~150MB | release/ |
| 模块加载 | < 500ms | 单个模块 |

---

## 最佳实践

### 代码规范
- ✅ 使用类型提示 (`typing`)
- ✅ 遵循 PEP 8
- ✅ 添加文档字符串
- ✅ 错误处理要完善

### 模块设计
- ✅ 保持模块独立性
- ✅ Logic 和 UI 分离
- ✅ 使用依赖注入
- ✅ 避免循环依赖

### UI 设计
- ✅ 响应式布局
- ✅ 支持多主题
- ✅ 键盘快捷键
- ✅ 无障碍支持

---

## 常见问题

### Q: 如何调试模块加载问题？
A: 查看日志文件 `logs/ue_toolkit_YYYYMMDD.log`

### Q: 如何重置配置？
A: 删除 `AppData/Local/UE_Toolkit/config/` 目录

### Q: 如何添加自定义主题？
A: 在 `resources/themes/` 中添加 JSON 配置文件

### Q: 模块之间如何通信？
A: 通过 `ModuleManager` 获取其他模块实例

---

## 项目统计

```
语言占比:
  Python:   95%
  QSS:      3%
  JSON:     2%

文件统计:
  Python 文件:   ~80 个
  QSS 文件:      ~15 个
  JSON 文件:     ~10 个

代码行数:
  核心系统:      ~3000 行
  功能模块:      ~8000 行
  UI 系统:       ~2000 行
  总计:          ~13000 行
```

---

## 未来规划

### 短期（1-2 月）
- [ ] AI 助手向量检索
- [ ] 插件市场
- [ ] 自动更新

### 中期（3-6 月）
- [ ] 云端同步
- [ ] 团队协作
- [ ] Web 端支持

### 长期（6+ 月）
- [ ] AI 代码生成
- [ ] 资产商店
- [ ] 企业版

---

**最后更新**: 2025-11-04  
**维护者**: UE Toolkit 开发团队

