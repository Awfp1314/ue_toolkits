# 🗂️ UE Toolkit - 项目结构

> **清理后版本** - 仅保留正式发布所需文件

---

## 📋 根目录

```
ue_toolkits - ai/
├── .gitignore                    # Git 忽略配置
├── build.py                      # PyInstaller 打包脚本
├── build_release.py              # 发布版本打包脚本
├── LICENSE                       # MIT 开源协议
├── main.py                       # 应用程序入口
├── README.md                     # 项目说明文档
├── RELEASE.md                    # 版本发布说明
├── requirements.txt              # Python 依赖列表
├── run_with_console.bat          # 带控制台启动脚本
├── run_without_console.bat       # 无控制台启动脚本
├── ue_toolkit.spec               # PyInstaller 配置文件
├── Ue_Toolkit使用文档.md         # 用户手册（简体）
└── 虚幻资产工具箱使用文档.md      # 用户手册（繁体）
```

---

## 🧩 核心系统 (`core/`)

### 核心模块
```
core/
├── __init__.py
├── app_manager.py               # 应用程序管理器
├── base_logic.py                # 基础逻辑类
├── config_manager.py            # 配置管理器
├── logger.py                    # 日志系统
├── module_interface.py          # 模块接口定义
├── module_manager.py            # 模块管理器
└── single_instance.py           # 单实例控制
```

### 配置系统 (`core/config/`)
```
core/config/
├── __init__.py
├── config_backup.py             # 配置备份
├── config_manager.py            # 配置管理
└── config_validator.py          # 配置验证
```

### 配置模板 (`core/config_templates/`)
```
core/config_templates/
└── app_config_template.json     # 应用配置模板
```

### 工具集 (`core/utils/`)
```
core/utils/
├── __init__.py
├── custom_widgets.py            # 自定义 Qt 控件
├── file_utils.py                # 文件操作工具
├── path_utils.py                # 路径处理工具
├── performance_monitor.py       # 性能监控
├── style_loader.py              # QSS 样式加载器（增强版）
├── theme_manager.py             # 主题管理器
├── thread_utils.py              # 线程工具
├── ue_process_utils.py          # UE 进程工具
└── validators.py                # 数据验证器
```

---

## 📦 功能模块 (`modules/`)

### AI 助手 (`modules/ai_assistant/`)
```
modules/ai_assistant/
├── __init__.py
├── __main__.py
├── ai_assistant.py              # 模块主入口
├── manifest.json                # 模块清单
│
├── logic/                       # 业务逻辑层
│   ├── __init__.py
│   ├── action_engine.py         # 动作执行引擎
│   ├── api_client.py            # API 客户端
│   ├── asset_reader.py          # 资产读取器
│   ├── audit_logger.py          # 审计日志
│   ├── config.py                # AI 配置
│   ├── config_reader.py         # 配置读取器
│   ├── context_manager.py       # 上下文管理
│   ├── controlled_tools.py      # 受控工具集
│   ├── conversation_memory.py   # 对话记忆
│   ├── document_reader.py       # 文档读取器
│   ├── enhanced_memory_manager.py  # 增强记忆管理
│   ├── intent_parser.py         # 意图解析器
│   ├── local_retriever.py       # 本地检索器
│   ├── log_analyzer.py          # 日志分析器
│   ├── memory_compressor.py     # 记忆压缩器
│   ├── remote_retriever.py      # 远程检索器
│   ├── runtime_context.py       # 运行时上下文
│   ├── site_reader.py           # 站点读取器
│   └── tools_registry.py        # 工具注册表
│
├── resources/themes/            # AI 助手主题
│   ├── dark.qss
│   └── light.qss
│
├── ui/                          # 用户界面层
│   ├── __init__.py
│   ├── chat_input.py            # 聊天输入框
│   ├── chat_input_bar.py        # 输入栏容器
│   ├── chat_window.py           # 聊天窗口主界面
│   ├── confirmation_dialog.py   # 确认对话框
│   ├── markdown_message.py      # Markdown 消息渲染
│   └── message_bubble.py        # 消息气泡
│
└── utils/
    └── module_theme.py          # 模块主题适配器
```

### 资产管理器 (`modules/asset_manager/`)
```
modules/asset_manager/
├── __init__.py
├── asset_manager.py             # 模块主入口
├── config_template.json         # 配置模板
├── manifest.json                # 模块清单
│
├── logic/                       # 业务逻辑层
│   ├── __init__.py
│   ├── asset_manager_logic.py   # 资产管理逻辑
│   ├── asset_model.py           # 资产数据模型
│   └── thumbnail_generator.py   # 缩略图生成器
│
└── ui/                          # 用户界面层
    ├── __init__.py
    ├── add_asset_dialog.py      # 添加资产对话框
    ├── asset_card.py            # 资产卡片组件
    ├── asset_manager_ui.py      # 主界面
    ├── category_management_dialog.py  # 分类管理对话框
    ├── confirm_delete_category_dialog.py  # 删除确认对话框
    ├── custom_checkbox.py       # 自定义复选框
    ├── dialogs.py               # 通用对话框
    ├── edit_asset_dialog.py     # 编辑资产对话框
    ├── first_launch_dialog.py   # 首次启动向导
    ├── progress_dialog.py       # 进度对话框
    └── set_paths_dialog.py      # 路径设置对话框
```

### 配置工具 (`modules/config_tool/`)
```
modules/config_tool/
├── __init__.py
├── __main__.py
├── manifest.json                # 模块清单
│
├── logic/                       # 业务逻辑层
│   ├── __init__.py
│   └── config_tool_logic.py     # 配置工具逻辑
│
└── ui/                          # 用户界面层
    ├── __init__.py
    ├── config_tool_ui.py        # 主界面
    ├── config_tool_ui_core.py   # 核心界面逻辑
    │
    ├── components/              # UI 组件
    │   ├── __init__.py
    │   ├── config_template.py   # 配置模板组件
    │   ├── config_template_button.py  # 模板按钮
    │   └── ue_project_card.py   # UE 项目卡片
    │
    ├── dialogs/                 # 对话框
    │   ├── __init__.py
    │   ├── config_apply_confirm_dialog.py  # 应用确认对话框
    │   ├── name_input_dialog.py  # 名称输入对话框
    │   ├── project_running_warning_dialog.py  # 运行警告对话框
    │   └── ue_project_selector_dialog.py  # 项目选择对话框
    │
    ├── handlers/                # 事件处理器
    │   ├── __init__.py
    │   └── config_handler.py    # 配置处理器
    │
    └── layouts/                 # 布局构建器
        ├── __init__.py
        └── layout_builder.py    # 布局构建器
```

### 站点推荐 (`modules/site_recommendations/`)
```
modules/site_recommendations/
├── __init__.py
├── __main__.py
├── manifest.json                # 模块清单
│
├── logic/                       # 业务逻辑层
│   ├── __init__.py
│   └── site_recommendations_logic.py  # 站点推荐逻辑
│
└── ui/                          # 用户界面层
    ├── __init__.py
    └── site_recommendations_ui.py  # 主界面
```

### 模块模板 (`modules/_template/`)
```
modules/_template/
├── __init__.py
├── __main__.py
├── manifest.json                # 模块清单模板
│
├── logic/                       # 业务逻辑层模板
│   ├── __init__.py
│   └── module_logic.py          # 模块逻辑模板
│
└── ui/                          # 用户界面层模板
    └── __init__.py
```

---

## 🎨 资源文件 (`resources/`)

### 图标资源
```
resources/
└── tubiao.ico                   # 应用程序图标
```

### QSS 样式系统 (`resources/qss/`)
```
resources/qss/
├── variables.qss                # 语义变量定义（中心配置）
│
├── components/                  # 组件样式
│   ├── buttons.qss              # 按钮样式
│   ├── chat_input.qss           # 聊天输入框样式
│   ├── config_tool.qss          # 配置工具样式
│   ├── confirmation_dialog.qss  # 确认对话框样式
│   ├── dialogs.qss              # 通用对话框样式
│   ├── main_window.qss          # 主窗口样式
│   ├── markdown_message.qss     # Markdown 消息样式
│   ├── scrollbars.qss           # 滚动条样式
│   └── title_bar.qss            # 标题栏样式
│
└── themes/                      # 主题配置
    ├── dark.qss                 # 深色主题
    └── light.qss                # 浅色主题
```

### 配置模板 (`resources/templates/`)
```
resources/templates/
└── global_settings.json         # 全局设置模板
```

### 主题配置 (`resources/themes/`)
```
resources/themes/
├── example_custom_theme.json    # 示例自定义主题
├── forest_green.json            # 森林绿主题
├── sunset_orange.json           # 落日橙主题
└── violet.json                  # 紫罗兰主题
```

---

## 🎨 主界面系统 (`ui/`)

```
ui/
├── __init__.py
├── settings_widget.py           # 设置面板
├── system_tray_manager.py       # 系统托盘管理器
├── ue_main_window.py            # 主窗口入口
├── ue_main_window_core.py       # 主窗口核心逻辑
│
├── dialogs/                     # 对话框
│   ├── __init__.py
│   └── close_confirmation_dialog.py  # 关闭确认对话框
│
├── icons/                       # 图标资源
│   └── toolbar.png              # 工具栏图标
│
├── main_window_components/      # 主窗口组件
│   ├── __init__.py
│   └── title_bar.py             # 自定义标题栏
│
└── main_window_handlers/        # 主窗口事件处理器
    ├── __init__.py
    ├── module_loader.py         # 模块加载器
    └── navigation_handler.py    # 导航处理器
```

---

## 🧰 开发工具 (`tools/`)

```
tools/
├── __init__.py
└── check_qss_variables.py       # QSS 变量一致性检测工具
```

---

## 📦 发布版本 (`release/`)

```
release/
├── run_with_console.bat         # 带控制台启动脚本
├── run_without_console.bat      # 无控制台启动脚本
├── version_info.txt             # 版本信息
│
└── ue_toolkit/
    ├── ue_toolkit.exe           # 主程序可执行文件
    └── _internal/               # PyInstaller 打包的依赖库
        ├── base_library.zip
        ├── python39.dll
        ├── PyQt6/               # PyQt6 库
        ├── core/                # 核心系统
        ├── modules/             # 功能模块
        ├── ui/                  # 主界面
        ├── resources/           # 资源文件
        └── [其他依赖库...]
```

---

## 📊 项目统计

### 文件统计
| 类型 | 数量 |
|------|------|
| **Python 源码** (`.py`) | ~150+ 个 |
| **QSS 样式** (`.qss`) | 13 个 |
| **配置文件** (`.json`) | ~10 个 |
| **文档** (`.md`) | 3 个 |
| **批处理脚本** (`.bat`) | 4 个 |

### 模块统计
| 模块 | 说明 | 文件数 |
|------|------|--------|
| `ai_assistant` | AI 聊天助手 | ~30 个 |
| `asset_manager` | 资产管理器 | ~15 个 |
| `config_tool` | 配置工具 | ~20 个 |
| `site_recommendations` | 站点推荐 | ~5 个 |

### QSS 样式系统
| 组件 | 文件数 | 说明 |
|------|--------|------|
| **变量定义** | 1 个 | `variables.qss` |
| **组件样式** | 9 个 | `components/*.qss` |
| **主题配置** | 2 个 | `themes/*.qss` |
| **自定义主题** | 4 个 | `themes/*.json` |

---

## 🎯 核心技术栈

### 框架与库
- **UI 框架**: PyQt6
- **Python 版本**: 3.9
- **打包工具**: PyInstaller
- **AI 集成**: OpenAI API / 本地嵌入模型

### 架构特点
- ✅ **模块化设计** - 可插拔的功能模块系统
- ✅ **主题系统** - 支持深色/浅色主题及自定义主题
- ✅ **QSS 样式系统** - 基于语义变量的统一样式管理
- ✅ **热重载支持** - QSS 样式实时更新（开发模式）
- ✅ **配置管理** - 集中化的配置与验证系统
- ✅ **日志系统** - 完整的应用日志记录
- ✅ **单实例控制** - 防止重复运行

---

## 📝 关键文件说明

### 应用入口
- **`main.py`** - 应用程序主入口，初始化核心系统和 QApplication

### 配置文件
- **`ue_toolkit.spec`** - PyInstaller 打包配置
- **`requirements.txt`** - Python 依赖列表
- **`resources/templates/global_settings.json`** - 全局设置模板

### 文档
- **`README.md`** - 项目说明与快速开始
- **`RELEASE.md`** - 版本发布说明
- **`Ue_Toolkit使用文档.md`** - 完整用户手册

### 样式系统
- **`resources/qss/variables.qss`** - QSS 语义变量中心配置
- **`core/utils/style_loader.py`** - 样式加载器（支持变量替换与热重载）
- **`core/utils/theme_manager.py`** - 主题管理器

---

## 🚀 项目特色

### 1. 模块化架构
每个功能模块独立封装，包含：
- `manifest.json` - 模块元数据
- `logic/` - 业务逻辑层
- `ui/` - 用户界面层
- 可热插拔，互不干扰

### 2. 统一样式系统
- **语义变量** - 所有颜色、尺寸统一定义在 `variables.qss`
- **组件化 QSS** - 每个组件有独立的 `.qss` 文件
- **主题切换** - 一键切换深色/浅色主题
- **热重载** - 开发时修改 QSS 立即生效

### 3. AI 智能助手
- **意图识别** - 自动解析用户意图
- **上下文管理** - 智能管理对话上下文
- **工具集成** - 与 UE 项目、资产、配置深度集成
- **记忆系统** - 长期记忆与对话历史压缩

### 4. 资产管理
- **自动分类** - 智能识别资产类型
- **缩略图生成** - 快速预览资产
- **批量导入** - 支持文件夹批量导入
- **独立主题** - 保留原有视觉风格

---

## ✨ 清理完成

**已删除文件类型：**
- ❌ 所有开发阶段的 README 文档（9 个，共 3705 行）
- ❌ 所有 QSS 重构过程文档（14 个）
- ❌ 所有测试文件（`tests/` 目录，13 个文件）
- ❌ 临时工具脚本（2 个）
- ❌ 旧版 QSS 文件（3 个）

**保留文件：**
- ✅ 所有 Python 源码
- ✅ 所有 QSS 样式文件
- ✅ 所有资源文件
- ✅ 正式用户文档（3 个）
- ✅ 长期维护工具（`check_qss_variables.py`）

---

*文档生成时间: 2025-11-04*  
*项目版本: 清理后正式发布版*

